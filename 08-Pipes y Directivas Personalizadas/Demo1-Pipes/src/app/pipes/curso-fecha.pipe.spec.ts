import { CursoFechaPipe } from './curso-fecha.pipe';

describe('CursoFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new CursoFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
